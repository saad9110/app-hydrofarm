<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hydrofarm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Forgetpassword"></activity>

        <service android:name=".NotifactionService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".PlantationTaskDetail" />
        <activity android:name=".ViewPlantationTask" />
        <activity android:name=".CreatePlantationTask" />
        <activity android:name=".GuideDetail" />
        <activity android:name=".ProductDetail" />
        <activity android:name=".ProductsandGuidesView" />
        <activity android:name=".Cart" />
        <activity android:name=".PlaceOrder" />
        <activity android:name=".UpdateProfile" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>